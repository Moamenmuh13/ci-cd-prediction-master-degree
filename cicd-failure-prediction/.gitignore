.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Everything else is tracked on purpose: data/raw, data/processed, models/,
# figures/, results/ and logs/. The thesis claims "full code, data, and
# trained models are committed" and reproducibility depends on it.
